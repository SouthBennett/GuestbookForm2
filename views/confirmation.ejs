<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GuestBook Confirmation</title>
  <link rel="stylesheet" href="styles/styles.css" />
</head>
<body>
  <div class="container">
    <h2>Thank you for signing my GuestBook!</h2>
    <p>Messaged Recieved. Your submission</p>
    <hr>

    <section>
      <!-- Show the submitted data dynamically -->
      <p><strong>First Name:</strong> <%= contact.fname %></p>
      <p><strong>Last Name:</strong> <%= contact.lname %></p>
      <p><strong>Job Title:</strong> <%= contact.jtitle || "-" %></p>
      <p><strong>Company:</strong> <%= contact.company || "-" %></p>
      <p><strong>Email:</strong> <%= contact.email || "-" %></p>

      <!-- Linkedin Section -->
      <% if (contact.linkedin) { %>
        <p><strong>Linkedin:</strong>
          <a href="<%= contact.linkedin %>" target="_blank"><%= contact.linkedin %></a>
        </p>
      <% } %>

      <p><strong>Connection:</strong> <%= contact.connection %></p>

      <% if (contact.other) { %>
        <p><strong>Other (details):</strong> <%= contact.other %></p>
      <% } %>

    </section>

    </hr>
    <a href="/" class="button">Back to form</a>
  </div>
</body>
</html>